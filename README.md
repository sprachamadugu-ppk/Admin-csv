# react-admin
This is a MERN project 

inorder to run the code,should follow the below steps
# 1.clone the repo in to local machine
#2.run npm install
#3.to run the frontend run the this commad npm run dev
#4.to run the backend tun this command npm run dev/npm run start

